/*
 * Requires: http://nodejs.org/#download or run in a browser
 * Usage: node hello.js 
 */

const x = 'Hello'
const y = 'World'

function hello_console() {
  console.log(x, y);
}

hello_console(x, y);
