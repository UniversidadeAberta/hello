/*
 * 'Hello World!' program in C++
 * Requires: g++ gcc
 * Compile:  g++ -g -Wall -o hello hello.cpp
 * Run:      ./hello
 * On Ubuntu/Linux just install gcc with apt
 * On Centos/Redhat/Fedore install with yum/dnf
 * On Windows download from http://www.mingw.org/
 * On a Mac use brew install
 */
 
#include <iostream>
 
int main()
{
  std::cout << "Hello World!" << std::endl;
  return 0;
}
